;;; Code style settings (e.g. indentation).

;; Change the Java indentation amount to 4 spaces
(setq c-default-style "java"
      c-basic-offset 4)

;; Set default fill-column to 100
(setq-default fill-column 100)

(provide 'style)

;;; style.el ends here
